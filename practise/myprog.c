#include <stdio.h>
	int main() {
    	// printf() displays the string inside quotation
    	printf("stdout is printed, good for you :) \n");
	fprintf( stderr, "stderr is printed, bad for you! :( \n");
     	return 0;
      	}

